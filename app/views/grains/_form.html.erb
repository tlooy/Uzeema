<%= form_for(@grain) do |f| %>
  <% if @grain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grain.errors.count, "error") %> prohibited this grain from being saved:</h2>

      <ul>
      <% @grain.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :participant_id %><br />
    <%= f.number_field :participant_id %>
  </div>
  <div class="field">
    <%= f.label :table_id %><br />
    <%= f.number_field :table_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
